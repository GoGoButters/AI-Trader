# AI-Trader Orchestrator - Quick Start Guide

## 🚀 Быстрый запуск (Развертывание системы)

### Предварительные требования

- Docker & Docker Compose
- Доступ к Graphiti серверу: `http://192.168.1.107:8001`
- Доступ к LLM API: `http://192.168.1.107:4000/v1`

### 1. Запуск всех сервисов

```bash
cd orchestrator
docker-compose up -d
```

Это запустит:

- **Perplexica** (порт 3000) - AI News Search
- **Orchestrator API** (порт 8080) - FastAPI backend
- **WebUI** (порт 3001) - Веб-интерфейс управления

### 2. Открыть WebUI

Откройте в браузере: **<http://localhost:3001>**

### 3. API Documentation

API Swagger Docs: **<http://localhost:8080/docs>**

## 📝 Настройка

### config.yml

Уже настроен с рабочими endpoint'ами:

- **Graphiti**: `http://192.168.1.107:8001`
- **LLM Models**: `http://192.168.1.107:4000/v1`
- **Perplexica**: `http://localhost:3000` (локально в Docker)

### Создание бота через WebUI

1. Заполните форму "Создать нового бота"
2. Укажите торговую пару (например, BTC/USDT)
3. Выберите таймфрейм и режим (demo/real)
4. Нажмите "Создать бота"

### Создание бота через API

```bash
curl -X POST "http://localhost:8080/api/bots/create" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "btc-demo-1",
    "pair": "BTC/USDT",
    "timeframe": "15m",
    "mode": "demo",
    "params": {
      "rsi_oversold": 30,
      "rsi_overbought": 70,
      "min_impact_score": 0.3,
      "enable_ai_analysis": true
    }
  }'
```

## 🔍 Проверка статуса

```bash
# Проверить запущенные контейнеры
docker ps

# Логи оркестратора
docker logs orchestrator-api

# Логи Perplexica
docker logs perplexica-search

# Логи WebUI
docker logs orchestrator-ui
```

## 🛑 Остановка

```bash
cd orchestrator
docker-compose down
```

## 🐛 Troubleshooting

### API не отвечает

Проверьте логи:

```bash
docker logs orchestrator-api
```

### Perplexica не работает

Проверьте доступность LLM API:

```bash
curl http://192.168.1.107:4000/v1/models
```

### Ошибка подключения к Graphiti

Убедитесь, что Graphiti запущен:

```bash
curl http://192.168.1.107:8001/health
```

## 📊 Мониторинг

WebUI автоматически обновляется каждые 5 секунд.
Вы увидите:

- Список всех ботов
- Статус каждого бота (running/stopped)
- Текущий профит
- Количество сделок

## ⚠️ Важно

- Всегда начинайте с **demo** режима
- Убедитесь, что Graphiti и LLM API доступны перед запуском
- WebUI работает только когда API запущен
